# @localization-governance/cli

Lifecycle operations and CI enforcement for localization governance.

```bash
locgov status es-MX --json
locgov ci --config localization-governance.config.mjs
```

The config module exports `{ service, actor, reviewer, requiredLocales }`.
