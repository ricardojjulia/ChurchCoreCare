# @localization-governance/provider-google

Google Cloud Translation provider adapter for localization governance.

```js
import { createGoogleTranslationProvider } from '@localization-governance/provider-google';

const google = createGoogleTranslationProvider({
  apiKey: process.env.GOOGLE_TRANSLATE_API_KEY,
});
```
