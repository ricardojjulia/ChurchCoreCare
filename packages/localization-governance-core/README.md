# @localization-governance/core

Framework-neutral localization catalog validation and governance lifecycle.

```js
import { createGovernanceService } from '@localization-governance/core';

const service = createGovernanceService({ storage, providers, policy });
```

The package does not depend on React, HTTP frameworks, databases, product
roles, or translation vendors.
